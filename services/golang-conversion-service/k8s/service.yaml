apiVersion: v1
kind: Service
metadata:
  name: conversion-service
  namespace: convertx
spec:
  selector:
    app: conversion-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP