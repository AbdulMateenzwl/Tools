# Populated by setup.sh from LOCALSTACK_AUTH_TOKEN in .env.
# The literal placeholder is never applied directly — see setup.sh.
apiVersion: v1
kind: Secret
metadata:
  name: localstack-auth
  namespace: localstack
type: Opaque
stringData:
  auth-token: "LOCALSTACK_AUTH_TOKEN_PLACEHOLDER"
