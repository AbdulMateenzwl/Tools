# Apply first: kubectl apply -f https://raw.githubusercontent.com/metallb/metallb/v0.14.5/config/manifests/metallb-native.yaml
# This file just documents the install step as a reference.
# The actual config is in ip-address-pool.yaml